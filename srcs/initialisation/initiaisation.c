bool	allocation()
{
	while < nb philo 
	if  create thread == SUCCESS
		cool
	exit




	resultat += (pthread create); +0
	resultat += (pthread create); +1
	resultat += (pthread create); +1
	resultat += (pthread create); 
	resultat += (pthread create); 
	resultat += (pthread create); 
	resultat += (pthread create); 
	resultat += (pthread create); 

	resultat == nb philo 

 }